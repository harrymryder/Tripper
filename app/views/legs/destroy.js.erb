if (document.getElementById("DLeg<%= @poi.id%>") !== null) {
  document.getElementById("DLeg<%= @poi.id%>").innerHTML = "<%= j render 'legs/create_button', trip: @trip, poi_id: @poi.id %>";
}

document.querySelector('.timeline-container').innerHTML = "<%= j render 'trips/trip_timeline', pois: @pois, first_poi: @first_poi %>"

document.getElementById('map').setAttribute('data-legs', "<%= @leg_list.to_json %>")
